# Development Environment Configuration
# Copy this file to .env for development setup
# Profile: development - Full-featured development with optional services

# ===== PROFILE SELECTION =====
MLOPS_PROFILE=development

# ===== REQUIRED SETTINGS =====

# Application Configuration
MLOPS_APP_NAME=ML Model Serving API
MLOPS_APP_VERSION=1.0.0-dev

# Server Configuration
MLOPS_HOST=0.0.0.0
MLOPS_PORT=8000

# ===== OPTIONAL SERVICE ENABLEMENT =====
# Enable these services as needed for testing

# Redis Configuration (disabled by default in dev)
# MLOPS_REDIS_ENABLED=true
# MLOPS_REDIS_HOST=localhost
# MLOPS_REDIS_PORT=6379
# MLOPS_REDIS_DB=0

# Kafka Configuration (disabled by default in dev)
# MLOPS_KAFKA_ENABLED=true
# MLOPS_KAFKA_BOOTSTRAP_SERVERS=localhost:9092
# MLOPS_KAFKA_CONSUMER_GROUP=kubesentiment_dev
# MLOPS_KAFKA_TOPIC=sentiment_requests_dev

# Vault Configuration (disabled by default in dev)
# MLOPS_VAULT_ENABLED=true
# MLOPS_VAULT_ADDR=http://localhost:8200
# MLOPS_VAULT_ROLE=dev-role
# MLOPS_VAULT_NAMESPACE=dev

# MLflow Configuration (disabled by default in dev)
# MLOPS_MLFLOW_ENABLED=true
# MLOPS_MLFLOW_TRACKING_URI=http://localhost:5000
# MLOPS_MLFLOW_EXPERIMENT_NAME=sentiment_analysis_dev

# ===== SECURITY SETTINGS =====
# Relaxed security for development

# API Key (optional in dev)
# MLOPS_API_KEY=dev-api-key-12345

# CORS Settings (permissive by default)
MLOPS_ALLOWED_ORIGINS=*
MLOPS_CORS_ORIGINS=*

# ===== MODEL CONFIGURATION =====

# Model Selection
MLOPS_MODEL_NAME=distilbert-base-uncased-finetuned-sst-2-english
MLOPS_ALLOWED_MODELS=distilbert-base-uncased-finetuned-sst-2-english,bert-base-uncased

# Model Caching
# MLOPS_MODEL_CACHE_DIR=./models/cache

# Text Processing
MLOPS_MAX_TEXT_LENGTH=512

# ===== MONITORING & LOGGING =====

# Logging (verbose in development)
MLOPS_LOG_LEVEL=DEBUG
MLOPS_ENABLE_METRICS=true

# Tracing (optional)
# MLOPS_ENABLE_TRACING=true
# MLOPS_TRACING_BACKEND=jaeger
# MLOPS_JAEGER_AGENT_HOST=localhost
# MLOPS_JAEGER_AGENT_PORT=6831

# ===== PROFILE DEFAULTS (REFERENCE ONLY) =====
# The development profile automatically sets:
# - MLOPS_DEBUG=true
# - MLOPS_WORKERS=1
# - MLOPS_ENVIRONMENT=development
# - External services disabled by default
# - Small prediction cache (100 items)
# - Verbose logging (DEBUG level)
#
# See app/core/config/profiles.py for complete defaults
