# Local Development Environment Configuration
# Copy this file to .env and customize as needed
# Profile: local - Minimal configuration for local development

# ===== PROFILE SELECTION =====
# Set to 'local' to use local profile defaults
MLOPS_PROFILE=local

# ===== REQUIRED SETTINGS =====
# These are the only settings you typically need to override

# Application Configuration
# MLOPS_APP_NAME=ML Model Serving API
# MLOPS_APP_VERSION=1.0.0

# Server Configuration (defaults to localhost)
# MLOPS_HOST=127.0.0.1
# MLOPS_PORT=8000

# ===== OPTIONAL OVERRIDES =====
# Uncomment and modify these only if you need to override profile defaults

# Security (local development - no auth required by default)
# MLOPS_API_KEY=your-local-api-key

# CORS (allows all origins by default in local)
# MLOPS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# Model Configuration
# MLOPS_MODEL_NAME=distilbert-base-uncased-finetuned-sst-2-english
# MLOPS_MODEL_CACHE_DIR=/tmp/model_cache

# ===== PROFILE DEFAULTS (DO NOT MODIFY) =====
# The local profile automatically sets these values:
# - MLOPS_DEBUG=true
# - MLOPS_LOG_LEVEL=INFO
# - MLOPS_WORKERS=1
# - All external services (Redis, Kafka, Vault) disabled
# - All MLOps features (MLflow, drift detection) disabled
# - Minimal caching for faster iteration
#
# See app/core/config/profiles.py for complete profile defaults
